import { MDXComponentPropsType } from './types';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function MdxClientUnifiedBundler(props: MDXComponentPropsType) {
  // todo: add bundler
}
